I'm futzing around with scrapy.

To get this running (after seven months away): 

`cd` to `scrapy-tutorial/tutorial`  
then:
```
$ pipenv shell
```
and then ... 
```
$ scrapy crawl healthspace
```
... and the stdout should fly!